import React from 'react';
import AuthBox from '../../shared/components/AuthBox';
const LoginPage = () => {
    return (
        <AuthBox>
            Login
        </AuthBox>
    );
};

export default LoginPage;